{
  "name": "shared-db-schema",
  "version": "1.0.0",
  "description": "Shared database schema for Rural Healthcare Platform",
  "main": "index.js",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:seed": "ts-node seed.ts",
    "db": "sudo docker run -d -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres",
    "postinstall": "prisma generate"
  },
  "keywords": [
    "prisma",
    "database",
    "healthcare"
  ],
  "author": "Rural Healthcare Team",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "@prisma/extension-accelerate": "^0.6.2",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "prisma": "^5.7.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
